import { BlackThemeDirective } from './black-theme.directive';

describe('BlackThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new BlackThemeDirective();
    expect(directive).toBeTruthy();
  });
});
